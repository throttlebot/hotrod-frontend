---
apiVersion: v1
kind: Service
metadata:
  name: hotrod-frontend
  labels:
    appName: hotrod-frontend
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the frontend service.
  ports:
  - name: hotrod-frontend
    port: 8080
  type: ClusterIP
  selector:
    appName: hotrod-frontend
  ports:
  - name: hotrod-frontend
    port: 8080
  type: LoadBalancer
  selector:
    appName: hotrod-frontend
---
apiVersion: apps/v1beta2 #  for k8s versions before 1.9.0 use apps/v1beta2  and before 1.8.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: GITLAB_CI_ENV_SLUG-hotrod-frontend
spec:
  selector:
    matchLabels:
      appName: hotrod-frontend
  replicas: REPLICA_COUNT
  template:
    metadata:
      labels:
        appName: hotrod-frontend
        track: APPLICATION_TRACK
        app: GITLAB_CI_ENV_SLUG
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
      - name: hotrod-frontend
        image: registry.hub.docker.com/willwangkelda/hotrod-frontend:COMMITID
        command: ["/go/bin/hotrod", "frontend"]
        env:
        - name: GET_HOSTS_FROM
          value: dns
        ports:
        - containerPort: 8080
